<header>
	<nav class="navbar navbar-expand-lg navbar-light">
		<div class="container mt-4">
			<div class="row">
				<div class="d-flex align-items-start">
					<a class="navbar-brand" href="#"><img src="/images/logo.jpg" alt="logo"></a>
				</div>
			</div>
			<div>
				<button class="navbar-toggler" type="button" data-bs-toggle="collapse"
					data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false"
					aria-label="Toggle navigation">
					<span class="navbar-toggler-icon"></span>
				</button>
				<div class="collapse navbar-collapse" id="navbarNavDropdown">
					<ul class="navbar-nav">
						<li class="nav-item">
							<a class="nav-link " href="/">ACCUEIL</a>
						</li>
						<li class="nav-item">
							<a class="nav-link " href="{{path('app_dish')}}">CARTE</a>
						</li>
						<li class="nav-item">
							<a class="nav-link " href="{{path('app_menu')}}">MENU</a>
						</li>
						<li class="nav-item">
							<a class="nav-link " href="{{path('app_reservations')}}">RESERVATION</a>
						</li>
						{% if not app.user %}
						<li class="nav-item">
							<a class="nav-link" href="{{path('app_login')}}">SE CONNECTER</a>
						</li>
						{% else %}
						<li class="nav-item dropdown">
							<a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown"
								aria-expanded="false">
								<img src="/images/flaticon/utilisateur.png" alt="user icon" width="17">
								<span class="userName">{{app.user.fullName}}
								</span>...
							</a>
							<ul class="dropdown-menu">
								{% if "ROLE_ADMIN" in (app.user.roles) %}
								<li>
									<a class="dropdown-item" href="{{ path('admin')}}">Administration</a>
								</li>
								{% endif %}
								<li>
									<a class="dropdown-item" href="{{path('app_logout')}}">Se déconnecter</a>
								</li>
							</ul>
						</li>
						{% endif %}
					</ul>
				</div>
			</div>
		</div>
	</nav>
</header>